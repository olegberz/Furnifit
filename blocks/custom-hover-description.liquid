<div class="ProductBreakdown_breakdown__TOBx_">
  {% for block in product.metafields.custom.product_breakdown %}
    <input type="radio" id="breakdown-{{ forloop.index }}" name="breakdown" class="hidden-radio" {% if forloop.first %}checked{% endif %}>

    <label for="breakdown-{{ forloop.index }}" class="ProductBreakdown_breakdown_item__GXcw9">
      <p class="type_base Type_headline5__do08K">{{ block.title }}</p>
      <p class="type_base Type_body2__7cQr6 ProductBreakdown_breakdown_item_description__B6LL5">
        {{ block.description }}
      </p>
    </label>
  {% endfor %}
</div>

<style>
/* Скрываем radio-кнопки */
.hidden-radio {
  display: none;
}

/* Стили для элементов */
.ProductBreakdown_breakdown_item__GXcw9 {
  cursor: pointer;
  padding: 10px;
  border: 1px solid #ddd;
  margin-bottom: 5px;
  transition: background 0.3s;
}

.ProductBreakdown_breakdown_item_description__B6LL5 {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease-in-out, padding 0.3s ease-in-out;
  padding: 0;
}

/* Когда radio выбран, описание раскрывается */
.hidden-radio:checked + label .ProductBreakdown_breakdown_item_description__B6LL5 {
  max-height: 200px; /* Можно подстроить под контент */
  padding: 10px 0;
}

/* Дополнительные эффекты */
.ProductBreakdown_breakdown_item__GXcw9:hover {
  background: #f9f9f9;
}
</style>
{% schema %}
  {
    "name": "Block name",
    "settings": []
  }
{% endschema %}